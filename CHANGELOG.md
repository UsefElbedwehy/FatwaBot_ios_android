# Changelog

All notable changes to this project are documented here. Format loosely follows [Keep a Changelog](https://keepachangelog.com/); milestone-based until first release.

## [Unreleased]

### 2026-07-12 — Backend FCM sender + campaign dispatch
- **`fcm_sender.ts`** — FCM HTTP v1 sender: mints a Google OAuth access token from the Firebase service account (RS256 JWT via `jose`, cached ~1h) and posts to `…/messages:send`; flags dead tokens (404/UNREGISTERED). `fetch`/`now` injectable → fully unit-tested with a generated key + fake fetch.
- **`notification_dispatch.ts`** — pure `dispatchCampaign`: per recipient it honors the user's per-type preference, enforces the daily cap (`notification_engine`), sends, writes the `delivery_log` (sent/capped/failed), and clears tokens FCM reports dead.
- **`POST /admin/v1/campaigns/{key}/send`** (admin) — loads campaign → template (locale-picked) → notification-type default → push audience (`identity.listPushTargets`, new repo method) → dispatch; returns `{recipients, sent, capped, failed, skipped}`. Returns `503` until `FCM_SERVICE_ACCOUNT` is set.
- Wired the optional `FcmSender` into `index.ts` from the `FCM_SERVICE_ACCOUNT` secret. New `tests/push_test.ts` (6 tests); full backend suite **105 green**. Deployed to the live function.
- To fire a real push: rotate + set the service-account secret, then trigger a campaign (docs/features/push-notifications.md).

### 2026-07-12 — FCM push client (Android wired end-to-end; backend token endpoint)
- **Backend**: new `PATCH /v1/me/push-token` endpoint stores the device's FCM token on `identity.devices.push_token` (authenticated; `updatePushToken` added to the identity repo + in-memory repo; 2 new tests, 11 passing). Deployed to the live function.
- **Android — fully wired & building**: added Firebase BOM + `firebase-messaging` + the `google-services` plugin (google-services.json already placed). `FatwaBotMessagingService` (`@AndroidEntryPoint`) registers new tokens with the backend (`PushTokenRegistrar` → authenticated `patchRaw`) and posts foreground messages on a `general` channel; `MainActivity` registers the token on launch; white mihrab-arch `ic_notification` set as the FCM default icon. Android push works end-to-end once the backend **sender** is wired.
- **Two gated pieces documented** (docs/features/push-notifications.md): (1) the backend **FCM sender** needs the Firebase service-account key set as a Supabase secret — and that key must be **rotated** first (it appeared in chat); (2) the **iOS client** is deliberately deferred — iOS push is impossible without the Apple Developer Program (APNs) and can't run on the Simulator, so the heavy `firebase-ios-sdk` wasn't added yet; the doc lists the small drop-in steps for when that account exists (the backend contract is already in place).
- Verified: backend `deno test` green; Android `:app:assembleDebug` + `./gradlew test` green.

### 2026-07-12 — Notification suite (both platforms)
- **Every notification is individually toggleable, with user-set offsets** (stakeholder direction). Extended the pure `NotificationPlanner` on both platforms from adhan + pre-adhan to four types:
  - **Adhan** — at each prayer time (toggle).
  - **Pre-adhan** — a user-set number of minutes *before* the adhan (toggle + stepper).
  - **Iqama reminder** — a user-set number of minutes *after* the adhan (toggle + stepper).
  - **Last third of the night** — one reminder at the start of the last third (Maghrib→Fajr × 2/3), computed on-device from the timeline (toggle).
- **Preferences persist** via a new `NotificationPreferenceStore` (UserDefaults on iOS, SharedPreferences on Android); editing in Settings saves and **reschedules immediately** through the ViewModel. `PrayerNotificationPreferences` moved from per-prayer sets to per-type booleans + clamped (1–60 min) offsets; schedulers clear the new `iqama-`/`lastthird-` id prefixes too.
- **Settings UI** (both platforms): a branded Notifications section (switches + −/+ minute steppers) and a **"?" Features Guide** — expandable rows explaining each notification/feature in plain language (stakeholder: a "?" per item describing what it does).
- New localized strings (ar/en) for the iqama + last-third notification templates and all the settings/guide copy.
- Planner tests rewritten + extended on both platforms (adhan-only, all-off, pre-adhan offset, iqama-after-adhan, last-third-at-2/3, past-drop, budget, stable ids). Verified: iOS `swift test` (PrayerKit + AppFeatures) green; Android `./gradlew test` green; both apps build.

### 2026-07-12 — Backend go-live wiring + Live Activity fix + streak brand mark
- **Supabase wired (API live):** deployed the `api` edge function to the real project (`nbeobnlgsbokomvkmzeq`), set the `API_JWT_SECRET`, and flipped both apps off the `api.invalid` placeholder onto `https://nbeobnlgsbokomvkmzeq.supabase.co/functions/v1/api`. Function verified reachable end-to-end (validates requests, reaches the DB). Remaining to fully activate: `supabase db push` (the 10 migrations) — deferred to the owner since it needs the DB password. Firebase client configs (`google-services.json`, `GoogleService-Info.plist`) placed; FCM SDK wiring is the next push step.
- **Live Activity double-activity bug fixed** (stakeholder: "one for Isha, another for Fajr"). Root cause: the `Activity` reference is in-memory only and is `nil` after relaunch, so `start()` requested a *second* activity while the previous prayer's was still on the Lock Screen. `SystemPrayerLiveActivityManager` now reconnects to `Activity.activities` (adopting the running one and ending duplicates) before deciding to start; `update()` also reconnects; `end()` clears all running activities.
- **Streak icon = the app logo, not fire** (stakeholder direction). New `BrandMark` component (filled mihrab arch) on both platforms replaces the flame in the streak hero ring and streak cards; section-header/empty-state fire glyphs swapped for neutral/brand ones.
- **AI fatwa search spec written** (docs/features/ai-search.md) — the M5 core: ingest → transcribe → chunk → embed (pgvector) → hybrid retrieval → cited, verbatim-quote answers from vetted scholars only. Flags the decisions needed before build (copyright/permission per source, transcription engine, LLM/embedding provider) and a phased plan. Full stakeholder brief + approved source list captured in docs/PRODUCT_REQUIREMENTS_2026-07.md.
- Verified: iOS `xcodebuild` (simulator) green; Android `:core:designsystem` + `:feature:gamification` compile green.

### 2026-07-12 — Prayer accuracy: default to Umm al-Qura + AlAdhan authority validation
- **Root-caused the 2–3 min discrepancy** a stakeholder saw vs. a popular reference app: the app's default calculation method was **MWL** (Muslim World League, Fajr 18°/Isha 17°), and since the backend config (which serves method per country) isn't deployed yet, **every** user silently fell back to MWL instead of the region-appropriate method. Not an on-device-vs-API problem — every source (our engine, AlAdhan, the other app) uses the same astronomical math; only the *method parameters* differed.
- **Fix:** changed the bundled default method from `mwl` to `umm_al_qura` on both platforms (`PrayerSettings.method`), so the offline fallback matches the Saudi authority most users compare against. The server config can still override method per country when it's live; the engine already supported Umm al-Qura, madhab, and per-prayer minute offsets.
- **Authority validation (new, closes the M0 corpus TODO):** the golden corpus only proved our ports match `adhan-js`. Added `content/test-fixtures/prayer-times/aladhan-umm-al-qura.json` — real Umm al-Qura timings pulled from the **AlAdhan API** (method=4, Makkah university) for 4 Saudi cities × 4 dates — plus `AlAdhanValidationTests` on **both** platforms asserting our on-device output matches the authority within 3 minutes. It actually matches to **≤1 minute** (pure rounding). The fixture is committed so the test runs offline.
- Decision recorded (docs/WORSHIP_TAB_DATA_MODEL.md Appendix A): prayer times stay **on-device** (accurate once tuned, offline, punctual, privacy-preserving) rather than moving to a server/API — an API would not be more accurate. A server FCM push is a *reliability backup for Android* (OEM-killed alarms) only, not a better primary; iOS local notifications already fire when the app is terminated.
- Verified: iOS `swift test` (PrayerKit + AppFeatures) green; Android `./gradlew test` green, including the new authority validation on both.

### 2026-07-11 — iOS premium visual overhaul: launch screen, app icon, brand design system, every screen redesigned
- **Branded launch screen + app icon** (both previously absent — the app flashed a blank white launch and shipped no icon). New vector assets generated from the mihrab-arch motif: a launch logo (arch + 8-pointed Islamic star + "Fatwa Bot" wordmark on cream) wired via a `LaunchScreen.storyboard` + `Assets.xcassets` (`INFOPLIST_KEY_UILaunchStoryboardName`), and a 1024px `AppIcon` (maroon field, cream arch, gold-bordered star). Generated deterministically via CoreGraphics scripts (no external design tooling).
- **New premium design-system library** (`DesignSystemKit/BrandKit.swift`) — the language that replaces stock `List`/`Form` chrome (stakeholder: screens "look like we are learning UIKit"): `brandScreenBackground` (warm cream gradient + faint arch watermark), `BrandCard` (elevated rounded card with soft shadow + hairline border), `BrandSectionHeader` (maroon→gold accent bar + icon), `RingProgress` (maroon→gold sweep ring for counters/missions/streaks), `RankMedal` (gold/silver/bronze leaderboard discs), `BrandEmptyState` (arch-badge empty states). All strictly theme-token driven, so the ADR-0011 server theme overlay still applies.
- **Every screen rebuilt off `List`/`Form` into custom `ScrollView` + card layouts:** Home (elevated Ask card + circular-badge quick actions on the branded background), Journey/Gamification (streak hero ring, mission progress rings, badge grid), Leaderboard (rank medals, per-board cards), Search History (day-grouped cards), Settings (profile hero card + branded sections), Azkar (category cards + ring-counter session), Dua (premium search bar + reverent reading card), Awrad (stat tiles + per-wird rings), Hadith (progress-ring collection cards + elevated reading card), Tasbeeh (ring counter + premium history), Qibla, and the city picker. The only remaining `Form` is inside the leaderboard join modal sheet (acceptable). The old `BrandedList.swift` stopgap helpers were removed.
- **Tooling fix:** `project.yml` now declares the `FatwaBot` scheme explicitly, so `xcodegen generate` always emits it as a shared scheme — permanently fixing the recurring "does not contain a scheme named FatwaBot" failure after a clean regenerate. `DEVELOPMENT_TEAM` stays blank (Xcode auto-signs from the logged-in account); a duplicate stray `FatwaBot 1.xcscheme` that caused an intermittent CodeSign failure was removed.
- Verified with real builds + tests: full `xcodebuild` (simulator) green, `swift test` 83 tests green; redesigned Home, Journey, and Settings screenshotted on the iPhone 17 simulator to confirm the premium look renders.

### 2026-07-11 — Android parity for the premium visual overhaul
- **Adaptive app icon + splash screen** (both previously absent — Android shipped the default icon and a plain window). New `mihrab_arch + 8-pointed star` vector drawable drives an adaptive icon (maroon background, cream arch foreground, `monochrome` layer for themed icons) and a splash window background (`Theme.FatwaBot.Splash` → cream field + centered logo, swapped to the app theme in `MainActivity.onCreate`), mirroring the iOS launch screen. Path data generated deterministically via a CoreGraphics script.
- **New Compose `BrandKit.kt`** (`:core:designsystem`) mirrors iOS `BrandKit.swift`: `Modifier.brandScreenBackground` (cream gradient + faint arch watermark via `drawBehind`), `BrandCard` (elevated shadow + border Surface), `BrandSectionHeader` (accent bar + icon), `RingProgress` (Canvas sweep-gradient ring), `RankMedal`, `BrandEmptyState`. All driven by `MaterialTheme.colorScheme` / the brand tokens.
- **Every Android screen rebuilt** off flat `LazyColumn`/Material defaults into the branded scroll + card language, matching the iOS redesign one-to-one: Home, Gamification/Journey (streak hero ring, mission rings, badge grid), Leaderboard (rank medals), Search History, Settings, Azkar (category cards + ring-counter session), Dua (premium search bar + reverent reading card), Awrad (stat tiles + per-wird rings), Hadith (progress-ring cards + elevated reading card), Tasbeeh (ring counter), Qibla (elevated compass card), and the city picker. The old `BrandedCard.kt` stopgap was removed.
- Verified with real builds + tests: `./gradlew :app:assembleDebug` and `./gradlew test` both green. (No Android emulator/system-image available in this environment for live screenshots; screens mirror the visually-confirmed iOS layouts using the identical component library.)

### 2026-07-11 — On-device feedback round: location/notification hang, premium onboarding + Worship + Settings redesign
- **iOS location-permission hang (root cause of both "location stuck" and "notification stuck" reports)** — `SystemLocationProvider.resolve()`'s `.notDetermined` case called `requestWhenInUseAuthorization()` then immediately fell through into `requestLocation()` before the OS dialog was answered; `requestLocation()` is a no-op pre-authorization, and the delegate's `locationManagerDidChangeAuthorization` never retried it after the user actually granted access — the continuation never resumed, hanging forever. Since Home's `.task` chain awaits location resolution alongside notification-permission request and config refresh sequentially, a stuck location resolve could present as "everything on the home screen is stuck." Fixed by tracking `awaitingAuthorization` and only calling `requestLocation()` once the delegate reports an actual authorized/denied outcome. Android's permission flow (UI-layer `ActivityResultContracts` launchers) was independently verified to not have this bug — no Android change needed.
- **Full-width, larger-padding buttons** for better tap targets — iOS onboarding buttons gained `.frame(maxWidth: .infinity)` + `.controlSize(.large)`; Android's onboarding buttons already used `fillMaxWidth()` but gained explicit `contentPadding(vertical = 16.dp)` to match.
- **Islamic "mihrab arch" motif in onboarding** — the pointed-arch shape specified in `docs/05_DESIGN_DIRECTION.md` since M0 but never implemented: new `MihrabArchShape`/`ArchIconBadge` (iOS, `DesignSystemKit`) and `mihrabArchPath`/`ArchIconBadge` (Android, `:core:designsystem`, `Shape` via `Path`) replace bare SF Symbol/Material icons in onboarding's Welcome/Highlights/Priming steps on both platforms.
- **Settings → Profile-first redesign** (stakeholder direction: "we will add login for sure and we need to add more things here") — both platforms gained a profile header section (avatar circle, "Guest", "Sign in — coming soon") above the existing settings content. The sign-in row is intentionally inert, not a fake-functional button — no real login exists yet (blocked on Q8 backend credentials). Android's Settings tab previously had **no real screen at all** (generic `ComingSoon` placeholder) — this is Android's first real Settings implementation, wired into `RootScaffold`.
- **Worship tab: list → premium grid redesign** ("worship design is not premium at all, make it grids of squares and amazing icons") — both platforms replaced the flat list of `NavigationLink`/`ListItem` rows with a 2-column grid of square tiles: filled icon in a rounded `primaryContainer` badge over a `surfaceElevated`/`surfaceContainer` card.
- **Prayer countdown Live Activity: on by default** (iOS-only, ADR-0016 originally specified off-by-default) — `UserDefaultsLiveActivityPreferenceStore` now registers a default of `true` via `UserDefaults.register(defaults:)`, while an explicit user opt-out is still respected. No Android equivalent exists (no Live Activity feature on Android).
- Verified with real builds + full test suites on both platforms: `swift test` (83 tests) + full `xcodebuild` app-target build, all green on iOS; `./gradlew test` + `:app:assembleDebug` (960 tasks), all green on Android.

### 2026-07-11 — Bug fixes: raw error text, widget contrast, generic-native branding
- **Raw network errors shown to users** — Gamification/Leaderboard/Search History surfaced `String(describing: error)` directly (e.g. a raw `URLError` transport failure), leaking internal detail and looking broken. New `Error.userFacingMessage` (CoreKit) maps to a friendly localized message (no connection / timed out / generic); all three ViewModels now use it. Root cause of "features not working": the backend isn't deployed yet (`AppEnvironment.apiBaseURL` is a placeholder, blocked on Q8 credentials) — this fix only cleans up how that failure is *displayed*, it doesn't (and can't) make the backend-dependent features work without a real deployment.
- **Widget white-text-on-light-background** — several widget `Text` views (`PrayerWidgets.swift`, `GamificationWidgets.swift`) had no explicit `.foregroundStyle`, inheriting the system default (`.primary`), which renders white in tinted/dark Home Screen widget appearance modes — unreadable against the widgets' always-light cream background. Fixed by giving every text element an explicit brand color.
- **Generic native look on M3/M4 screens** — Prayer/Tasbeeh (M1/M2) already used the branded cream surface + rounded elevated cards; Gamification/Journey, Leaderboard, Search History, and the new Settings/Worship-list screens (M3/M4) used plain `List`/`LazyColumn` with no branding, rendering iOS/Android system-default chrome instead. Not a new design decision — the tokens were already approved in `docs/05_DESIGN_DIRECTION.md`, just not applied consistently.
  - **iOS**: new `brandedListBackground`/`brandedRowCard` `View` extensions (`DesignSystemKit/BrandedList.swift`) — cream `List` background + rounded `surfaceElevated` per-row cards — applied to `GamificationScreen`, `LeaderboardScreen`, `SearchHistoryScreen`, the new `SettingsScreen`, and `WorshipTabView`.
  - **Android**: new `BrandedCard` composable (`:core:designsystem/BrandedCard.kt`) — rounded `surfaceContainer` card matching Prayer's existing `Surface(shape = RoundedCornerShape(...))` pattern — applied to the same five screens' Android equivalents (`GamificationScreen`, `LeaderboardScreen`, `SearchHistoryScreen`, `WorshipTab`'s menu list). Android's `MaterialTheme` colorScheme was already correctly wired to the brand tokens app-wide (`FatwaBotTheme` maps `background`/`surface`/`primary` from `LightTokens`/`DarkTokens`), so this was a lower-severity gap than iOS's (flat unstyled rows on an already-correct background, not wrong colors) — still fixed for full visual parity between platforms.
- Verified with real builds + full test suites on both platforms: `swift test` (82 tests) + full `xcodebuild`; `./gradlew test` + `:app:assembleDebug` (960 tasks), both green.

### 2026-07-08 — M4: iOS Live Activity — prayer countdown (ADR-0016)
- **New `FatwaBotLiveActivity` app-extension target** (`ios/LiveActivity/`), added alongside the existing `FatwaBotWidgets` extension per ADR-0016 — local-only ActivityKit, zero push/APNs. Renders the countdown to the next prayer on the Lock Screen and in the Dynamic Island (expanded, compact, and minimal presentations) via `Text(timerInterval:)`, which the system animates client-side with no app wake-ups between updates.
- **`PrayerActivityAttributes`** (PrayerKit, `#if canImport(ActivityKit) && os(iOS)`-guarded since ActivityKit isn't available on the plain-macOS platform these packages also target for `swift test`): static `locationName` attribute + a `ContentState` of exactly `{ prayerName, prayerTime }`, per the ADR.
- **`PrayerLiveActivityManaging` boundary** (PrayerFeature, mirrors the existing `HeadingProviding`/`#if canImport` seam in `QiblaScreen`) keeps `PrayerViewModel` and its unit tests free of any ActivityKit dependency; `SystemPrayerLiveActivityManager` is the real iOS-only implementation, wired in only by `AppContainer`.
- **Opt-in, off by default** (`UserDefaultsLiveActivityPreferenceStore` — `UserDefaults.bool(forKey:)` returns `false` when unset, so no explicit default-registration needed): the Settings tab — previously a bare "coming soon" placeholder with zero real content — gained its first real screen, a single toggle. Disabling ends the activity immediately rather than waiting for the next state transition to notice.
- **`PrayerViewModel` wiring**: `apply(location:)` and `refreshNextPrayer()` (already-existing state-transition hooks — a new "next prayer" resolved, or a timer-tick/foreground refresh) now also sync the Live Activity when enabled, mirroring exactly the widget-snapshot update pattern from task 42 (skipped on the synchronous cold-start init path for the same reason: it's a system-IPC side effect, not needed to unblock first paint, and the async `start()` re-apply moments later still updates it).
- **Simplification vs. the ADR's literal wording on "explicitly ends it at local midnight":** there's no existing day-boundary/midnight timer in `PrayerViewModel` to hook an explicit `end()` call into (the ADR's phrasing assumed one existed). Since `apply()`/`refreshNextPrayer()` already re-sync the activity to whatever the *current* next-prayer state is on every foreground/tick, a day boundary is naturally handled the same way — the activity just starts reflecting tomorrow's Fajr once the app re-enters and recomputes, same as `today`/`tomorrow` do today. `ActivityKit`'s own `staleDate` (30 min after the countdown target) remains the safety net for the case where the app never re-enters before the target passes.
- New tests assert: off by default; enabling starts the activity with the current next-prayer; disabling ends it immediately; a `refreshNextPrayer()` tick also re-syncs while enabled. (Testing a fire-and-forget `Task {}` launched from `@MainActor` code needed a small polling `waitUntil` helper rather than a single `Task.yield()` — one yield doesn't reliably let the spawned task complete before assertions run, which showed up as session-run-to-run test flakiness before the fix.)
- Verified with real builds: `swift test` (82 tests, stable across repeated runs) + full `xcodebuild` app-target build, which now also compiles and embeds the new `FatwaBotLiveActivity.appex` into `FatwaBot.app/PlugIns`.

### 2026-07-08 — M4: Performance pass (cold start, widget battery)
- Static-code performance audit (no device/Instruments profiling available in this environment — every finding is a file-verified static analysis, not a runtime trace) on both platforms, independently cross-verified by multiple agent passes plus direct manual reading of the cited files.
- **Fixed on both platforms:** `PrayerViewModel.init`'s cached-location fast-path (the "instant first paint from cache, refine async" pattern) was also synchronously triggering a widget-snapshot write — a second 3-day prayer-timeline recomputation, a disk write to the app-group/shared-prefs container, and a `WidgetCenter.reloadAllTimelines()` / Glance `updateAll()` IPC call — all before the tab bar's first frame. Added an `updateWidget` parameter to `apply(location:)` (default `true`), passed `false` from the synchronous `init` cached-location path only; `start()`'s subsequent async re-apply (which already runs moments later on every launch) still updates the widget normally, so widgets stay just as fresh — the fix only removes this work from the *blocking* part of cold start.
- **Investigated and deliberately left alone** (real but low/negligible-impact — flagged rather than "fixed" to avoid churn/risk for sub-millisecond wins): `ConfigService`/`HomeViewModel`'s eager synchronous config-snapshot file read on first Home-tab construction (both platforms — same architecture, same tiny JSON file, same "offline-first instant-then-refine" pattern already accepted elsewhere), the onboarding-completion-check synchronous file read gating first composition/render (both platforms, sub-100-byte file), and Android's `EncryptedPrefsAuthTokenStore` eager Keystore/`MasterKey` construction (only reached on first Journey-tab visit, not app cold start).
- **Widget battery: no defects found on either platform.** Both `WidgetCenter.reloadAllTimelines()` call sites (iOS) and both `updateAll()` call sites (Android) are gated to once-per-meaningful-change (once per location resolve/settings change for prayer, once per `load()` for gamification), not per state mutation. Android's Glance `updatePeriodMillis` (30 min) is already at the OS-enforced practical floor. All widget `getTimeline`/`provideGlance` implementations on both platforms read only pre-computed snapshot files — zero network or heavy computation in the widget process, as designed since M1/M3.
- New tests on both platforms specifically assert the fixed behavior: cached-location construction does not trigger a widget write/reload, but the subsequent `start()` call still does.
- Verified with real builds + full test suites on both platforms: `swift build` + `swift test` (78 tests) + full `xcodebuild` app-target build, all green on iOS; `./gradlew test` + `:app:assembleDebug` (960 tasks), all green on Android.

### 2026-07-08 — M4: Accessibility audit (VoiceOver/TalkBack, Dynamic Type, reduced motion)
- Ran independent iOS + Android accessibility audits, cross-checked every finding against live file state before acting (both reports were fresh and accurate this time, unlike the stale iOS report earlier in M4's empty-states audit).
- **Reduced motion — the single biggest gap, now fixed on both platforms:** iOS gained a reusable `motionAnimation(_:value:)` view modifier (`DesignSystemKit/ReducedMotion.swift`) that silently skips the animation when `accessibilityReduceMotion` is on; wired into every `MotionTokens`-backed call site plus the two `withAnimation` sites (Qibla compass rotation, Prayer day-navigation) that predated `MotionTokens`. Android had **no reduced-motion signal anywhere in the codebase** — added `LocalReduceMotion`/`rememberReduceMotion()`/`motionAnimationSpec()` (`:core:designsystem`, reads the system's `Settings.Global.ANIMATOR_DURATION_SCALE` "remove animations" setting, the closest Android equivalent to iOS's API), provided once at `AppRoot` so it's available everywhere, and wired into all 5 existing animation call sites (Qibla, Tasbeeh, Azkar, Awrad, Gamification).
- **VoiceOver/TalkBack labels added** for every icon-only interactive control the audits found actually missing one: Prayer's day-navigation chevrons (both platforms), Azkar's translation toggle, Tasbeeh's history button, Dua's favorite/transliteration toggles, Awrad's per-wird increment button, Android's JourneyTab/WorshipTab back arrows. (Several audit-flagged "missing" labels on Android — Awrad's stats/add toolbar buttons — turned out to already be correctly labeled on file-state verification; left untouched.)
- **Composite-row grouping** (`.accessibilityElement(children: .combine)` / `Modifier.semantics(mergeDescendants = true)`) added to the highest-traffic rows so VoiceOver/TalkBack read them as one stop instead of 2-3 disjoint fragments: Prayer's times list, Gamification's streak/mission/badge rows, Leaderboard's board-summary/entry rows and join-dialog switch, Awrad's wird/stat rows, Qibla's static-fallback and accuracy-indicator views, plus Gamification's badge earned/locked state (previously conveyed by icon alone — added an explicit accessibility value on both platforms).
- **Dynamic Type / font-scale safety** on the two fixed-circle tap targets (Tasbeeh, Azkar): iOS uses `@ScaledMetric`/`.minimumScaleFactor`; Android grows the circle by the system font-scale setting (capped at 1.6×) since Compose's `Text` has no direct minimum-scale-factor equivalent in this project's Compose version.
- **Explicitly deferred** (flagged, not silently dropped): a real `error` field surfaced distinctly from "empty" in Azkar/Dua/Hadith/Awrad `UiState` (same gap already deferred from the empty-states audit — same root cause, same fix needed); the long tail of secondary composite rows not on the highest-traffic list (Hadith reading screen's entry-number/benefit-note groupings, Dua/Awrad create-sheet template rows, widget rows); a `SpacingTokens` object (the Android audit noted color/shape/motion tokens exist but spacing doesn't — unrelated to this task's scope, noted for a future design-system pass).
- Verified with real builds + full test suites on both platforms: `swift build` + `swift test` (77 tests) + full `xcodebuild` app-target build, all green on iOS; `./gradlew test` (full project) + `:app:assembleDebug`, all green on Android — this is the first M4 task where Android was actually build-verified rather than hand-reviewed, since `JAVA_HOME` was sorted out this session (Homebrew's `openjdk@21` was already installed from an earlier session but keg-only/not on PATH, so a plain `java -version` and filesystem search both missed it).

### 2026-07-08 — M4: Empty states, micro-interactions, animation audit
- Ran a cross-platform audit (independent iOS + Android passes, cross-checked against actual file state to filter out stale findings) covering every M2/M3-era feature for three gaps: a distinct "loaded but genuinely empty" state vs. an indefinite spinner, haptic feedback on count/completion actions, and animated transitions on changing values.
- **Empty states fixed (both platforms):** Azkar, Hadith, Dua category/collection lists now distinguish "still loading" from "loaded, nothing there" with a dedicated message instead of a spinner that never resolves; Tasbeeh history and Dua's top-level (non-search) list gained the same treatment they were missing entirely.
- **Haptics added for previously-silent actions (both platforms), reusing the existing `HapticsProviding` boundary (CoreKit / `:core:common`) already wired for Tasbeeh/Azkar/Dua/Awrad:** Hadith's mark-entry-read (fires only on newly-read entries, not revisits — mirrors Awrad's dedup-on-crossing pattern), Search History's delete/clear-all, Leaderboard's join (fires only on server success, not on a failed join).
- **New shared `MotionTokens`** (iOS `DesignSystemKit` / Android `:core:designsystem`, mirrors the existing `ColorTokens`/`ShapeTokens` pattern): `quickDuration`/`QUICK_MS` (0.2s, small value changes) and `standardDuration`/`STANDARD_MS` (0.3s, section/screen transitions). Backed animated counters in Awrad (both platforms, already had Android-only before this pass), Tasbeeh (Android — iOS already animated), Azkar's tick counter (both platforms, newly added), and Gamification's mission progress bar (both platforms). Also fixed Android's `QiblaScreen` needle animation, which had a hardcoded `tween(150)` predating the token.
- **Explicitly deferred** (lower-usage-frequency actions, flagged for a future pass rather than silently dropped): Gamification badge-unlock/mission-complete haptics and animation, Dua's loading-state `Text` vs `CircularProgressIndicator` inconsistency (cosmetic), and a proper `error` field in Azkar/Dua/Hadith/Awrad `UiState` (currently a failed content load renders identically to "genuinely empty" — would need `ContentService` to surface failure rather than silently returning empty, a bigger change than this pass's scope).
- Verified with real builds + full test suites on both platforms: `swift build` + `swift test` (all packages, 77 tests) + full `xcodebuild` app-target build all green on iOS; `JAVA_HOME=$(brew --prefix openjdk@21)/libexec/openjdk.jdk/Contents/Home ./gradlew test` (full project, all modules) + `:app:assembleDebug` both green on Android.

### 2026-07-08 — M4: Onboarding flow (iOS + Android)
- **New `OnboardingCompletionStore`** (iOS CoreKit / Android `:core:common`): a single local boolean+timestamp file, checked once at launch to gate whether onboarding shows — no backend, no account needed (onboarding runs before any identity exists). A reinstall sees onboarding again, by design.
- **iOS `OnboardingFeature` package**: `OnboardingViewModel` (4-step state machine: welcome → highlights → location priming → notification priming) takes the actual permission-request calls as injected closures rather than depending on `PrayerFeature`'s `LocationProviding`/`PrayerNotificationScheduling` directly (ADR-0010 forbids feature→feature deps) — the App composition root wires them to the real M1 code paths. `OnboardingScreen` respects Reduced Motion (slide transitions collapse to a fast fade). New `AppRootView` gates between `OnboardingScreen` and the existing `RootTabView`.
- **Android `:feature:onboarding` module**: same 4-step `OnboardingViewModel`, but since Android's runtime-permission dialogs can only be launched from a Composable (`rememberLauncherForActivityResult`), the ViewModel owns navigation/completion-persistence only, while `OnboardingScreen` itself launches `ACCESS_COARSE_LOCATION`/`POST_NOTIFICATIONS` (API 33+ only) requests and calls back into the ViewModel. New `AppRoot` composable (Hilt `@EntryPoint` to reach the store before any `@Composable` ViewModel exists) gates between `OnboardingScreen` and the existing `RootScaffold`.
- Neither platform touches Home's own already-existing permission-request calls (`PrayerViewModel.start()` / `RootScaffold`'s `LaunchedEffect`) — both are naturally idempotent once a permission is answered, so declining in onboarding cleanly falls through to the same manual-city/notifications-off experience that existed before onboarding shipped.
- Verified with real builds + full test suites on both platforms (9 new iOS tests, 8 new Android tests) — `xcodebuild` (full app + widget extension) and `./gradlew assembleDebug` + `test` (all modules) both green.

### 2026-07-08 — M4 planning (Premium polish pass)
- Resolved two long-open stakeholder questions gating M4 scope: **Q2** — "Fatwa Bot" is the final public name (store listing proceeds under it); **Q7** — Arabic + English at launch, translation pipeline proven in beta before further locales.
- Stakeholder approved the iOS Live Activity prayer countdown for M4 scope. Wrote [ADR-0016](docs/adr/ADR-0016-live-activity-prayer-countdown.md): local-only ActivityKit extension, zero push/APNs infrastructure — the countdown target is known a full day in advance (prayer times are computed on-device per ADR-0003), so `Text(timerInterval:)` lets the system animate the countdown with no app wake-ups; `update()` fires only at existing next-prayer state transitions. Opt-in via a Settings toggle, off by default.
- Wrote [docs/features/onboarding.md](docs/features/onboarding.md): 4-screen value-first flow (welcome → what-you'll-find → location priming → notification priming), 100% local UI sequencing over already-existing M1 permission-request code paths — no new backend, no new permission-handling logic. Every priming screen is skippable except the final screen; declining any permission still reaches a fully-usable Home (manual-city fallback, notifications simply off).
- Registered the remaining M4 work as tasks: onboarding implementation, empty-states/micro-interaction/animation audit, accessibility audit, performance pass, Live Activity implementation, Firebase Crashlytics+Analytics wiring, beta program + store listing prep. The last two remain blocked on Q8 (Firebase project, Apple Developer, Google Play Console credentials) — everything else is unblocked and ready to implement.

### 2026-07-08 — Dashboard: gamification/leaderboard/notification CRUD + user management — M3 COMPLETE
- **Generic CRUD extended to 7 new admin collections** (streak-defs, missions, badges, leaderboard-defs, notification-types, notification-templates, notification-campaigns) by adding `CollectionDef` entries — zero new backend work needed, since these already rode task 27's generic `/admin/v1/content/{collection}` CRUD (the ADMIN_COLLECTIONS reuse pattern paid off again).
- **New dashboard field kinds**: `boolean` (checkbox), `json` (raw-JSON textarea for point formulas/criteria/eligibility — edited as data, not code, so a bad edit is fixable by re-editing the row), `array` (one-value-per-line, for `event_types`/`tie_breakers`), `optional-text` (nullable text fields — submits `null` not `""` so nullable `timestamptz` columns like `starts_at` don't fail to cast on an empty submit; kept distinct from plain `text` specifically so existing M2 content fields' empty-string behavior wasn't disturbed).
- **New Gamification and Notifications landing pages** (`/gamification`, `/notifications`) linking into the shared `/content/{slug}` editor; `nav.ts` domains marked `implemented`.
- **New Users domain** (`/users`): the first surface that ISN'T admin-authored content — real `identity.users` account data. Required genuinely new backend work: `GET /admin/v1/users?query=&limit=&before=` (paginated, search by display_name substring or exact id), a dedicated `AdminUsersRepo`/`SupabaseAdminUsersRepo`, 4 new router tests. UUID-shaped queries also match by exact id; non-UUID queries only match display_name (avoids a Postgres cast error on `id.eq.<garbage>`).
- **Verified end-to-end in a real browser**, not just unit tests: stood up a throwaway local gateway (real `router.ts` + in-memory repos, same reusable pattern from task 27) and drove the actual Next.js dev server through login → create a streak-def with an array field → create a leaderboard-def with a JSON point-formula field → search the Users page — confirmed the full round-trip (form → server action → parse → backend → re-render) for every new field kind, not just that it type-checks.
- **Backend**: 4 new tests (97 total, up from 93), OpenAPI (`admin.v1.yaml`) updated with `/admin/v1/users` + `AdminUser` schema.
- **M3 is now fully complete** (tasks 28–37): accounts, gamification, leaderboards, search history, notifications — backend, iOS, Android, widgets, and dashboard all shipped and verified. Next: M4 planning.

### 2026-07-08 — Streak + Daily Challenge widgets (iOS + Android)
- **New `GamificationWidgetSnapshot`** (iOS CoreKit / Android `:core:common`, mirrors `PrayerWidgetSnapshot`): the app precomputes and writes just the single most relevant streak (longest current length) and daily mission (first not-yet-complete, falling back to the first if all are done) after every successful `GamificationViewModel.load()` — widgets render with zero network access, same discipline as the M1 prayer widgets.
- **iOS:** `StreakWidget` + `DailyChallengeWidget` (`systemSmall`, WidgetKit) added to the existing `FatwaBotWidgets` bundle; widget extension gained a `CoreKit` dependency. Reused/promoted the prayer widgets' brand-color constants to file-internal so all widgets share one source of truth.
- **Android:** `StreakWidget` + `DailyChallengeWidget` (Glance) added to the `:widget` module alongside the existing prayer widgets, registered as receivers in the shared manifest, reusing the existing small-widget-info XML.
- Verified with real full builds on both platforms (`xcodebuild` including the widget extension target; `./gradlew assembleDebug` + full test suite) — caught a real Hilt DI mistake this way: an injected `() -> Long` clock lambda had no `@Provides` binding and would have failed at Hilt component-compile time; simplified to a direct `System.currentTimeMillis()` call since no test needed clock control here, rather than adding an unnecessary binding.
- **M3 now fully complete except dashboard CRUD** (task 37: gamification/leaderboard/notification admin collections + user management).

### 2026-07-07 — Leaderboard + search-history UI (iOS + Android)
- **Leaderboard:** a single generic renderer (per docs/features/leaderboard.md) drives every board regardless of `scope`/`period` — no per-board client code. Join flow shows the pseudonymous handle first with "publish my name instead" as an explicit secondary toggle, and a city field that only appears for `scope: city` boards. Leave is a one-tap action; both flows reload from the server afterward rather than trusting client-side state as authoritative.
- **Search history:** new `SearchHistoryRecording` boundary (CoreKit / `:core:common`, mirrors `ActivityEventRecording`) so searchable features never depend on the search-history feature module directly (ADR-0010). History screen groups entries by day, supports per-entry delete (optimistic, rolls back on failure) and a confirm-gated clear-all.
- **Scoping finding:** audited all three features the spec assumed had search boxes (Azkar, Dua, Hadith Collections) on both platforms — only **Dua** actually has one. Wired search-history recording there only; Azkar/Hadith get it automatically once they grow a search box, since the boundary is already in place. Recording dedupes identical consecutive queries (not a full debounce) rather than firing on every keystroke.
- **New feature modules:** iOS `LeaderboardFeature` + `SearchHistoryFeature` packages; Android `:feature:leaderboard` + `:feature:searchhistory` modules — both mirror the `GamificationFeature`/`:feature:gamification` structure and DI pattern established last session.
- **Bug caught by actually compiling, not just writing tests:** repeated the earlier-documented "private response DTO breaks test fakes" mistake on iOS (`LeftResponse`/`DeletedResponse`/`ClearedResponse` needed to be internal, not private, for `@testable import` fakes to construct them) — caught immediately because the build was run, not assumed.
- Verified with real builds on both platforms: iOS `xcodebuild` full app build succeeded; Android `./gradlew test` (all modules, including 2 new ones) + `:app:assembleDebug` succeeded, catching and fixing a missing `@OptIn(ExperimentalMaterial3Api::class)` and an icon-import path issue in the new Journey-tab navigation.
- **Remaining M3 work:** streak/daily-challenge widgets (iOS + Android), dashboard CRUD for gamification/leaderboard/notification admin collections + user management.

### 2026-07-07 — Android gamification UI (parity with iOS)
- **Closed a pre-existing client gap on both platforms:** neither iOS nor Android had any auth-networking layer despite M1's backend anonymous-auth having shipped months earlier. Built the Android side (`:core:network`): `EncryptedPrefsAuthTokenStore` (Android Keystore-backed, the Keychain equivalent), `AuthService` (anonymous sign-in + single-use refresh-token rotation, 60s proactive-refresh buffer, falls back to a fresh anonymous identity if refresh is rejected), `AuthenticatedApiClient` (bearer attachment, single 401→refresh→retry). Snake_case wire DTOs mirror the OpenAPI `TokenResponse` shape exactly (`user_id`, `access_token`, `expires_in`, `refresh_token`).
- **New `:feature:gamification` module:** `GamificationEventRecorder` (the concrete `ActivityEventRecording` — queues locally first so `record()` never blocks/fails visibly, flushes opportunistically, batch-submits to `/v1/gamification/events`, idempotent-retry safe), `GamificationViewModel` (renders the server's streaks/missions/badges verbatim — no client-side scoring math), `GamificationScreen` (Compose, wired into the previously-placeholder Journey tab).
- **Activity-event wiring across all 4 content features** (parity with iOS): Tasbeeh fires on `completeSet()` (guarded by count > 0), Azkar on real `completeSession()` (not the idempotent re-tick), Awrad fires separately for `tick()` and `markDayComplete()`, Hadith fires only on newly-read entries (gated on Kotlin `Set` insert, not on revisits) — each with a dedicated test proving the gating, not just "doesn't crash."
- **Verified with a real build, not just claims:** no JDK was preinstalled in this environment — found and used Homebrew's `openjdk@21`. Full `./gradlew test` (all modules) and `:app:assembleDebug` both green; caught and fixed two real Hilt DI graph errors (missing binding for the concrete `GamificationEventRecorder` type consumed alongside the `ActivityEventRecording` interface) and a JVM name-mangling crash from a semicolon inside a backtick-named test function — neither would have surfaced without actually compiling.
- **Remaining M3 work:** iOS + Android leaderboard/search-history UI, streak/challenge widgets, dashboard CRUD for the M3 admin collections.

### 2026-07-07 — Milestone 3 backend complete (accounts, gamification, leaderboards, search history, notifications)
- **Specs first:** wrote all 5 M3 feature specs before code; resolved OPEN_QUESTIONS Q2c/Q2d/Q4/Q5 with the stakeholder (opt-in-only city leaderboards, 2/day notification cap, Fajr-to-Fajr admin-editable day boundary, in-app-actions-only streaks — never self-reported prayer logging).
- **Accounts (ADR-0004 extension):** `/v1/auth/apple`, `/v1/auth/google` sign-in and `/v1/auth/link` anonymous→account upgrade, both behind a pluggable `IdentityProviderVerifier` (dev-stub today, real Apple/Google JWKS verification later with no contract change — blocked only by Q8 credentials). Linking preserves `user_id`, so no local/synced state is lost. Optional self-chosen `display_name` via `PATCH /v1/me/profile`.
- **Gamification engine (ADR-0007/0012):** idempotent batched activity-event ingest; pure streak-folding logic (`gamification_engine.ts`) — day-boundary approximated as an admin-configurable fixed local time (real Fajr needs on-device location the backend doesn't have), grace tokens that preserve but don't extend a streak and replenish outside a rolling window. `GET /v1/gamification/profile` assembles streaks/missions/badges live from the event log + currently-published definitions — no separately-drifting cache.
- **Leaderboard engine (ADR-0012):** weighted/capped point formulas, ranking with ordered tie-breakers, pseudonymous-by-default join/leave/membership (real name shown only if the board requires it or the member opts in), admin-triggered snapshot recompute standing in for `pg_cron` until Supabase is deployed.
- **Search history:** per-user, paginated, source-filterable CRUD; reserved AI sources (`ai_fatwa`/`ai_hadith`/`ai_question`) unused until M5.
- **Notifications:** admin-editable catalog + per-user preference overrides; pure cap-check logic (2/day default, admin-configurable, campaign-only — never applies to locally-computed worship/gamification reminders). Actual FCM dispatch is explicitly deferred (needs Firebase credentials + audience-segment querying beyond this milestone) — catalog, prefs, delivery-log schema, cap logic, and open-tracking are all real and tested, ready for a dispatch handler once those land.
- **Key architectural continuity win:** every new definition table (streak_defs, missions, badges, leaderboard_defs, notification_types, templates, campaigns) reuses task 27's generic `/admin/v1/content` CRUD instead of a parallel bespoke API — extended `ADMIN_COLLECTIONS` to carry a `(schema, table)` pair per collection instead of an assumed single schema.
- **Totals:** 93 backend tests (up from 76), all green; every migration (0007–0010) verified against a throwaway local Postgres; OpenAPI specs (mobile + admin) updated throughout; lint/fmt clean at every step.
- **Remaining M3 work:** iOS/Android gamification + leaderboard + search-history UI, streak/challenge widgets, and dashboard CRUD for the new admin collections.

### 2026-07-04 — Planning phase
- Transcribed the foundation PDF into canonical [prompts/00_PROJECT_FOUNDATION.md](prompts/00_PROJECT_FOUNDATION.md).
- Added planning package: project analysis, architecture, architecture review, roadmap (M0–M7), design direction + Home screen spec, ADR-0001…0010 (all Proposed), open questions, future improvements, README.
- Flagged missing `design/` concept-demo assets and absent git initialization.
- **Gate:** awaiting stakeholder approval before Milestone 0 implementation.

### 2026-07-04 — Planning pass 2 (design review + configurability requirements)
- Reviewed the 22 concept screens added in `App Demo design/` → [docs/06_DESIGN_REVIEW.md](docs/06_DESIGN_REVIEW.md); adopted the FATWA BOT brand (maroon/cream, mihrab-arch motif) into the design direction.
- New modules from the designs: **Awrad (personal wird routines)** and **Hadith Collections (الأربعون learning)** — added to roadmap M2; rejected the demo's in-app secret-code admin panel.
- New ADRs for the backend-configurability philosophy: ADR-0011 (server-driven config, theme, strings, Home layout), ADR-0012 (leaderboards/streaks/missions as data), ADR-0013 (notification campaign engine), ADR-0014 (multi-locale content).
- Updated architecture (config platform §3a, schema domains, campaign engine, AI provider routing), roadmap (M0–M3 scope), open questions (Q1 resolved; new Q2b–Q2d).
- **Gate:** awaiting stakeholder approval of pass-2 updates before Milestone 0.

### 2026-07-04 — Final approval & implementation start
- Stakeholder approved architecture, roadmap, and documentation; all ADRs 0001–0014 marked **Accepted**.
- Final directives folded in: ADR-0015 (configurable-by-default + tenancy-ready schemas); Home spec rebalanced for the dual identity (AI assistant + daily companion — AI section above the fold, daily hadith/dua, featured content, quick actions/widget shortcuts sections added to the catalog); ADR-0008 extended (provider priority, fallback chains, sampling params, safety policies, cost metering); ADR-0013 extended (segmentation, delivery/open analytics, A/B-ready templates); ADR-0009 dashboard scope = operational control center; CMS block model (rich text, images, video, audio, PDF, markdown, sanitized HTML); analytics pipeline domain added.
- Repository initialized under git. **Milestone 0 implementation begins.**

### 2026-07-04 — Milestone 0 complete (local)
- **Backend:** config-platform migrations (apps/ADR-0015, config domain, identity skeleton), seed data, Edge Function API gateway (`/v1/health`, `/v1/config`, `/v1/config/theme`, `/v1/config/strings/{locale}`, `/v1/home`, `/v1/config/prayer-defaults`), OpenAPI v1 contract, 11 deno tests green.
- **iOS:** FatwaBotKit (CoreKit contract models, NetworkingKit APIClient, DesignSystemKit tokens with server-theme overlay) — 9 tests green; XcodeGen app target with themed 4-tab shell, ar/en + RTL; simulator build green.
- **Android:** version-catalog Gradle setup, Hilt, `:core:common` contract models (2 tests green), `:core:designsystem` Material3 brand theme, 4-tab Compose shell, ar/en + RTL; full `gradlew build` green (JDK 21 + SDK 35 provisioned).
- **Dashboard:** Next.js control-center shell with domain navigation and backend health probe; lint/typecheck/build green.
- **Prayer spike (ADR-0003):** 140-entry golden corpus (adhan-js reference) reproduced by adhan-swift within ±90s; qibla bearings ±1°. **Finding:** ≥48° latitude requires an explicit high-latitude rule — library defaults diverge by hours across ports; policy encoded in `config.prayer_defaults` and the corpus.
- **CI:** paths-filtered workflows for all four platforms (activate when a GitHub remote is added).
- M1 feature specs pre-written: config-sync, prayer.
- **Blocked on credentials (Q8):** Supabase project (deploy `/v1/health` + migrations), Firebase, store accounts, GitHub remote for CI.

### 2026-07-05 — Milestone 1 in progress (6 of 9 tasks complete)
- **Backend auth (ADR-0004):** `/v1/auth/anonymous` (device-registered identity, HS256 access JWT + hashed single-use refresh tokens), `/v1/auth/refresh` rotation with replay rejection, `/v1/me` bearer probe; migration 0004; 16 deno tests green.
- **iOS ConfigSync (ADR-0011 client half):** ConfigKit — snapshot store (atomic, app-group-ready), per-layer independent refresh, delta string packs, flag gating with `min_app_version`; 6 tests per spec.
- **Prayer engines on both platforms:** settings (clamped adjustments, Hijri offset), day/timeline, next-prayer across midnight/Isha boundaries, Hijri dates; **high-latitude auto rule stated identically** (≥48° → seventh-of-the-night); iOS 11 + Android 11 tests green, both passing the 140-entry golden corpus.
- **UI slices on both platforms:** server-layout Home renderer (native section catalog v1: header/hero/ask/quick-actions, unknown types skipped), gradient prayer hero with live countdown + 5-prayer strip, Ask section with the three intents + trust line, Prayer screen with day pager and Hijri header, manual-city fallback (12 bundled cities), iOS Qibla compass with calibration/interference states; Factory (iOS) and Hilt (Android) composition; both apps build green.
- Remaining M1: local notification engines, widgets, Android ConfigSync port.

### 2026-07-05 — Milestone 1 complete
- **Android ConfigSync port (ADR-0011):** `:core:network` (OkHttp ApiClient + client-context headers), `:core:config` (ConfigService with parallel per-layer refresh, delta string packs, min_app_version flag gating, atomic FileConfigStore); Home now renders the real server layout + flags. 6 tests mirroring the iOS spec cases. Full ConfigSync parity between platforms.
- **Local notification engines (ADR-0013):** pure `NotificationPlanner` on both platforms (rolling window, per-prayer adhan + pre-adhan offsets, budget cap, stable ids) with 5 mirrored tests each; iOS scheduler over UNUserNotificationCenter, Android over AlarmManager + a boot-surviving BroadcastReceiver; reschedule wired into both ViewModels; adhan texts in ar/en.
- **Widgets v1 (ADR-0003):** shared `PrayerWidgetSnapshot` (precomputed 48h, app-group/file store) written by the app; iOS WidgetKit extension (Next Prayer, Timeline, Hijri) and Android Glance (Next Prayer, Hijri) reading it with zero network. 3 tests each platform.
- **M1 exit criteria met:** a user in any city gets correct offline prayer times, qibla, adhan notifications, and a working widget — with zero sign-up. Totals: iOS 41 tests, Android 26 tests, backend 16 tests, all green; both apps build.
- **Still blocked on credentials (Q8)** for deployment/CI only — all M1 features work locally offline-first.

### 2026-07-06 — Milestone 2 in progress (6 of 11 tasks complete)
- **Content pipeline (ADR-0014):** `content` schema (azkar/dua categories+items, hadith collections+entries, wird templates — all multi-locale jsonb translations, versioned publishing); real seed content (Hisnul-Muslim-style morning/evening/after-prayer azkar, Quranic + daily-occasion duas, 3 entries of Nawawi's 40 with benefit notes, 3 wird templates) validated against a throwaway local Postgres. `GET /v1/content/{azkar,duas,hadith-collections[/{slug}],wird-templates}` — locale-resolved, delta-aware (`since_version`); 9 backend tests.
- **Client content sync (both platforms):** ContentKit/`:core:content` mirror ConfigSync's offline-first pattern — bundled seed JSON fallback, per-collection independent cache, silent failure. Bundled seed generated from the same backend seed data (single source).
- **Tasbeeh (fully local):** presets, custom dhikr, configurable target, haptics (tick + distinct target-reached), history/stats; hoisted `HapticsProviding` to CoreKit/`core:common` so later features can share it without a feature→feature dependency (ADR-0010). 8 tests each platform.
- **Azkar:** session state machine (auto-advance at exact repeatCount, same-day resume, idempotent completion), category list with completed-today badge, reading screen (virtue-note card, calm completion state per ADR-0007 — no gamified burst). 6 tests each platform.
- **Dua library:** browse/search/favorites; favorites keyed by stable duaId (survives content resync, not array position); diacritic-insensitive Arabic search distinguishing "not searching" from "no matches"; reading view with share. 7 tests each platform.
- **Found & fixed:** ContentKit's server-contract structs had no explicit `public init` — Swift's synthesized memberwise init for a public struct is only internal, silently working via Codable but unconstructible from feature-module tests/fixtures. Also two recurring iOS/macOS cross-compile snags (`navigationBarTitleDisplayMode`, `.listStyle(.insetGrouped)` are iOS-only; AppFeatures builds for macOS too for fast `swift test` runs) — both guarded with `#if os(iOS)`.
- Worship tab now hosts real Prayer/Qibla/Tasbeeh/Azkar/Dua destinations on both platforms (two-level back navigation for list↔detail features); Hadith Collections remains the last "coming soon" row.
- Remaining M2: Awrad, Hadith Collections, final Worship/Home quick-actions wiring, Admin Dashboard v1 (auth + CMS).

### 2026-07-06 — Milestone 2 in progress (10 of 11 tasks complete)
- **Awrad:** template-guided wird creation, local daily ticking, day-completion requiring all active wirds to hit target, archiving that preserves historical stats, lifetime aggregation (dhikr/days/Qur'an-pages/salawat) mirroring the concept demo's stat row. 8 tests each platform.
- **Hadith Collections:** browse/read Nawawi's-40-style collections, progress keyed by entry number (a set, no double-counting), resumes at last-read entry, prev/next clamped at boundaries, jump-to-entry. 6 tests each platform.
- **Worship tab + Home quick actions wired (both platforms):** all seven مزايا worship modules (Prayer, Qibla, Azkar, Dua, Tasbeeh, Awrad, Hadith) now reachable from the Worship tab; Home's quick actions deep-link directly into a screen (iOS: NavigationPath + navigationDestination; Android: hoisted destination state), not just switch tabs.
- **Found & closed a real parity gap:** Android never had a Qibla screen at all (iOS had it since M1) or a Home quick-actions row (silently dropped with a "arrives in M2" comment) — both built now: SensorManager rotation-vector heading provider + animated compass (Android), QuickAction enum + row (Android), completing platform parity for the entire مزايا section.
- Remaining M2: Admin Dashboard v1 (auth + content CMS).

### 2026-07-06 — Milestone 2 complete
- **Admin Dashboard v1 (ADR-0009), backend:** `/admin/v1` surface on the same gateway function — self-issued admin JWTs (`aud=admin`, distinct from mobile tokens), generic draft/publish/version CRUD across the 7 whitelisted content collections, audit log on every mutation. Version bumps only once a row has been published, so draft iteration doesn't churn the client-visible version. `admin.admin_users` / `admin.audit_log` (migration 0006, RLS deny-by-default); `openapi/admin.v1.yaml`; bootstrap dev admin seeded (`admin@fatwabot.dev`, dev-only password, `on conflict do nothing`). 15 new backend tests (76 total); lint/fmt clean.
- **Admin Dashboard v1, UI:** real Next.js pages replacing the M2 placeholder — login (server action + httpOnly session cookie), content collection index/list/editor (locale tabs sourced from `/v1/config`, not hardcoded) with Save/Publish/Unpublish, audit log with collection filtering. `proxy.ts` (Next 16's renamed middleware convention) gates the authenticated routes; a defense-in-depth 401 handler clears the session and redirects if a token expires mid-session.
- **Verified end-to-end** against a throwaway local gateway (real router + in-memory repos, no live Supabase needed): login → list → edit → save (no version bump on a draft) → publish → edit again (version bump confirmed) → audit log (all mutations recorded, most recent first) → create draft. typecheck/lint/build all green.
- **M2 exit criteria met:** all seven مزايا worship modules complete and wired on both platforms with parity; content is admin-authored end-to-end (create → edit → publish → visible on `/v1/content/*`) with a full audit trail.
- **Still blocked on credentials (Q8)** for deployment/CI only — all M0–M2 features work locally offline-first or against the local dev backend.
