<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:localeConfig="@xml/locales_config"
        android:name=".FatwaBotApplication"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.FatwaBot">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.FatwaBot.Splash"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Widget deep links — keep the scheme in sync with
                 com.fatwabot.core.common.DeepLink.SCHEME -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="fatwabot" />
            </intent-filter>
        </activity>

        <!-- AlarmManager loses every alarm on reboot, and a schedule computed
             against the old clock is wrong after a timezone change. Neither was
             handled: RECEIVE_BOOT_COMPLETED was declared but nothing listened. -->
        <receiver
            android:name="com.fatwabot.app.notifications.ScheduleRestoreReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.fatwabot.feature.prayer.PrayerAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".notifications.ContentReminderAlarmReceiver"
            android:exported="false" />

        <!-- Posts the daily "did you complete [wird]؟" notification. -->
        <receiver
            android:name=".notifications.WirdReminderAlarmReceiver"
            android:exported="false" />

        <!-- Where the نعم / لاحقاً buttons land. exported="false": only our own
             PendingIntents may fire it, so nothing outside the app can mark a
             wird complete. -->
        <receiver
            android:name=".notifications.WirdReminderActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.fatwabot.app.WIRD_REMINDER_DONE" />
                <action android:name="com.fatwabot.app.WIRD_REMINDER_LATER" />
            </intent-filter>
        </receiver>

        <service
            android:name=".push.FatwaBotMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />

        <!-- Persists the in-app language choice on API 26–32, where the
             platform has no per-app locale store. On API 33+ the OS handles it
             (see android:localeConfig above) and this is inert. Without it the
             picker would apply the locale for the session and forget it on the
             next cold start. -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>
    </application>
</manifest>
